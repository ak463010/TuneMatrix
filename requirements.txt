PySide6>=6.7
numpy>=1.26
librosa>=0.10.2
audioflux>=0.1.9
soundfile>=0.12.1
pyrubberband>=0.3.0
demucs>=4.0.1

# Optional preferred key-detection backend:
# 1. install Cython first:
#    python -m pip install Cython
# 2. then install madmom without build isolation:
#    python -m pip install --no-build-isolation madmom

# Install these system tools separately and keep them on PATH:
# ffmpeg
# rubberband
